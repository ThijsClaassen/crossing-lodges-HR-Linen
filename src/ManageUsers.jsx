// SUPERSEDED — not used anywhere in this app (no longer imported by
// App.jsx).
//
// User management moved to a single centralized screen in the Finance
// Dashboard app (Thijs, 2026-08-09), so there's one Users tab for every
// app instead of a separate one per app. See instead:
//
//   crossing-lodges-budget/src/ManageUsers.jsx
//
// (crossing-lodges-budget is the Finance Dashboard app's repo, inside your
// CL Dashboard folder.) Safe to delete this file — kept only so the diff
// history shows what changed and why, rather than a change with no trace.
export default function ManageUsers() {
  return null
}
